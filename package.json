{
  "name": "mini-hcm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"npm:dev:frontend\" \"npm:dev:vercel\"",
    "dev:frontend": "cd frontend && npm install && npm start",
    "dev:vercel": "vercel dev --listen 3000",
    "start": "node -e \"console.log('This project is intended to be run on Vercel or via vercel dev')\""
  },
  "dependencies": {
    "axios": "^1.4.0",
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "firebase-admin": "^11.11.0",
    "luxon": "^3.4.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "vercel": "^30.0.0"
  }
}
